#Não é possível alterar uma lista de strings podemos alterá-las métodos, o primeiro deles é: replace = trocar/reposicionar
# Var.replace(' python, android') --> Ele sibistituirá python por android
# var.upper() -->Esta função substitui as letras minusculas por maiúsculas. O que for grande ele mantém, o que fro
#peqeno ele troca
#Var.lower() --> Contrário de upper()
#Var.capitalize() --> joga todos os caracteres em minuśculo e mantém o promeiro em maiusculo
#Var,title() --> Analisa quantas palavras tem na string, Todos os caracteres após o espaço é convertido pra maiuscul

# var.Strip()--> strip remove os caracteres desnecessŕios, antes e após da string
# rstrip() (r de rigth (direita) --> remove somente os espaços da direita
# lstrip() (l de left (esquerda) --> remove somente os espaços da esqueda
