# Podemos dividir string
# Var.split() -> (Como fazer split?) O Split refaz os indices a partir dos espaços, ele os deleta, assim criando um
# novo caracte, colocando cada uma dessas palavras em uma oura lista

# O split fará uma lista com todas as palavras em uma cadeia de caracters, ele divide uma lista separada por caracter
# Em duas listas,e gera uma nova lista para armazená-las por exemplo:

# Um vetor que armazena uma frase, com duas palavras, onde há um espaço entre elas. A função split caçará o espaço
# divirá a frase em duas palavras com quatro indices, que por sua vez, serão adicionadas a outros dois novos vetores
# que receberão uma numeração de 1 e 2

#Juncão '-'.join(var) --> este junta as strings, gera uma únicas string, onde pode-se concatenar por meio da
#sring que adicionar ao join para juntá-las