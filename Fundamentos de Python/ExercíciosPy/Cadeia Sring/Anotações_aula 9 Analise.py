# Usamos a função len para análisar a quantidade de caracters var.len('')
# Usamos o count para contar quantas vezes aparece um determinado caracter var.count('o')
# var.count('o',0,13) é possível fazer a contagem já com o fatiamento
#var,find('deo') find = encontrar --> comando que mostra quantas vezes é possível encontrar determinado pedido.
# E indica o indice da posição onde começou
#quando colocamos uma string que não exise no find,ele retorna o valor -1

#Além do find, podemos usar o operador "in" para análise ex: "'curso' in frase", contudo ele não mostra o indice
#ele trabalha com valores booleanos -> não é funcionalidade// é um operador.